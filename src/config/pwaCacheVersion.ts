/**
 * Bump when shipped static assets or cache behavior should change.
 * Drives Workbox `cacheId` / runtime cache names and client preload localStorage keys.
 */
export const PWA_CACHE_VERSION = "v1";
